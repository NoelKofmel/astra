# Local development: copy to .env (never committed). The values match
# infra/docker-compose.dev.yml. Every app parses what it needs in its config
# module and refuses to start on anything missing or malformed.

LOG_LEVEL=debug

DATABASE_URL=postgres://astra:astra@localhost:5432/astra
REDIS_URL=redis://localhost:6379
